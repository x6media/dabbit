<UserControl x:Class="dabbit.Win.ServerChannelNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
             mc:Ignorable="d"
              MouseRightButtonUp="TextBlock_MouseRightButtonUp" TouchDown="TextBlock_TouchDown" TouchUp="TextBlock_TouchUp">
    <Grid Background="LightBlue" Width="Auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <TextBlock Text="{Binding Path=ChannelName}" Grid.ColumnSpan="2" Grid.Row="0" Width="Auto"/>
        
        <Button x:Name="rejoinBtn"  Content="Rejoin" Grid.Column="1" Grid.Row="1" Visibility="Collapsed" />
        <Button x:Name="closeBtn" Content="Close" Grid.Column="1" Grid.Row="2" Visibility="Collapsed"  />
    </Grid>
</UserControl>
